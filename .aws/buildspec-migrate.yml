version: 0.2
# Stage: Migrate DB
# Registra nueva task definition con la imagen recién subida y corre
# alembic upgrade head como ECS task independiente.
# Variables inyectadas: ECR_REPOSITORY_URI, ECS_CLUSTER, ECS_SERVICE,
#                       TASK_DEF_FAMILY

phases:
  build:
    commands:
      - |
        CURRENT_TASK=$(aws ecs describe-task-definition \
          --task-definition $TASK_DEF_FAMILY \
          --query 'taskDefinition' --output json)

        NEW_TASK=$(echo "$CURRENT_TASK" | jq \
          --arg IMG "$ECR_REPOSITORY_URI:latest" \
          --arg CTR "backend" \
          '(.containerDefinitions[] | select(.name==$CTR) | .image) |= $IMG |
           del(.taskDefinitionArn,.revision,.status,.requiresAttributes,
               .compatibilities,.registeredAt,.registeredBy,.enableFaultInjection)')

        NEW_TASK_ARN=$(aws ecs register-task-definition \
          --cli-input-json "$NEW_TASK" \
          --query 'taskDefinition.taskDefinitionArn' --output text)

        echo "Task definition registrada: $NEW_TASK_ARN"

        NET_CFG=$(aws ecs describe-services \
          --cluster $ECS_CLUSTER --services $ECS_SERVICE \
          --query 'services[0].networkConfiguration' --output json)

        TASK_ARN=$(aws ecs run-task \
          --cluster $ECS_CLUSTER \
          --task-definition $NEW_TASK_ARN \
          --launch-type FARGATE \
          --network-configuration "$NET_CFG" \
          --overrides '{"containerOverrides":[{"name":"backend","command":["alembic","upgrade","head"]}]}' \
          --query 'tasks[0].taskArn' --output text)

        echo "Esperando migración: $TASK_ARN"
        aws ecs wait tasks-stopped --cluster $ECS_CLUSTER --tasks $TASK_ARN

        EXIT=$(aws ecs describe-tasks \
          --cluster $ECS_CLUSTER --tasks $TASK_ARN \
          --query 'tasks[0].containers[0].exitCode' --output text)

        [ "$EXIT" = "0" ] || { echo "Migración falló (exit $EXIT)"; exit 1; }
        echo "Migración OK."
